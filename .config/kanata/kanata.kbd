(defcfg
  process-unmapped-keys yes
)

(defsrc
  caps
)

(deffakekeys
  ctrl-mode lctl
)

(defalias
  caps-ctrl (multi (on-press-fakekey ctrl-mode press) (on-release-fakekey ctrl-mode release))
)

(deflayer base
  @caps-ctrl
)

